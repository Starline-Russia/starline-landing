# V3 Hero Copy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the v3 Hero headline and supporting message with the approved GMV-focused positioning while preserving the existing Hero composition, CTA, proof metrics, and responsive behavior.

**Architecture:** Keep `src/components/Hero.astro` as the single source of Hero markup for both the full landing and `/preview/hero/`. Lock the approved copy and responsive type sizes in the existing structural test before changing the component and its Hero-specific rules in `src/styles/global.css`.

**Tech Stack:** Astro `7.0.9`, strict TypeScript, plain CSS, Node.js built-in test runner

## Global Constraints

- Preserve Astro `7.0.9`, strict TypeScript, and plain CSS; add no dependency or JavaScript.
- Keep one `h1`, exactly one Hero CTA labeled `Оставить заявку`, and the existing three proof metrics.
- Keep the CTA, proof metrics, background, orbits, grid, spacing, and centered composition unchanged.
- Keep the two headline phrases in separate elements; the second phrase retains the violet gradient treatment.
- Use `22px` for the supporting message above `640px` and `17px` at `640px` and below.
- Do not add a final period to the approved supporting message.
- Do not synchronize the working project into the installed `starline-landing-v3` skill; that requires a separate explicit request.
- Run npm commands with the bundled Node runtime because the current system Node binary cannot load `libsimdjson.30.dylib`:

```bash
PATH=/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/override:$PATH
```

---

### Task 1: Refresh the shared Hero copy and responsive typography

**Files:**
- Modify: `../tests/v3-landing.test.mjs:78-140`
- Modify: `src/components/Hero.astro:7-14`
- Modify: `src/styles/global.css:113-118`
- Modify: `src/styles/global.css:425-431`

**Interfaces:**
- Consumes: the existing `<Hero />` imports in `src/pages/index.astro` and `src/pages/preview/hero.astro`
- Produces: one shared semantic Hero with the approved two-part `h1`, approved supporting paragraph, `22px` tablet/desktop paragraph type, and `17px` mobile paragraph type

- [ ] **Step 1: Update the structural test with the approved Hero contract**

In `../tests/v3-landing.test.mjs`, replace the two old headline assertions inside
`v3 contains the approved section order and hero contract`:

```js
assert.match(markup, /Starline — оператор роста/);
assert.match(markup, /для электронной коммерции/);
```

with:

```js
assert.match(markup, /Управляем маркетингом/);
assert.match(markup, /как системой роста GMV/);
```

Then add these assertions to `hero has isolated responsive styling and a
standalone preview`, immediately after the existing `assert.doesNotMatch` for
decorative stars:

```js
assert.match(hero, /<span>Управляем маркетингом<\/span>/);
assert.match(hero, /<strong>как системой роста GMV<\/strong>/);
assert.match(
  hero,
  /Связываем привлечение новых клиентов, аналитику и каналы продаж с ростом GMV —\s*а не только с показателями в рекламных отчётах\s*<\/p>/s,
);
assert.doesNotMatch(
  hero,
  /показателями в рекламных отчётах\.\s*<\/p>/s,
);
assert.match(
  css,
  /\.hero-copy > p\s*\{[^}]*font-size:\s*22px/s,
);
assert.match(
  css,
  /@media \(max-width: 640px\)[\s\S]*?\.hero-copy > p\s*\{[^}]*font-size:\s*17px/s,
);
```

- [ ] **Step 2: Run the source tests and verify the new contract fails**

Run:

```bash
PATH=/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/override:$PATH npm test
```

Expected: FAIL in the two Hero-related tests because `Hero.astro` still contains
the old headline and paragraph, the base paragraph rule still uses
`clamp(17px, 1.5vw, 22px)`, and the mobile override still uses `16px`.

- [ ] **Step 3: Apply the minimal shared-component and CSS change**

In `src/components/Hero.astro`, replace only the current `h1` and its following
paragraph with:

```astro
<h1>
  <span>Управляем маркетингом</span>
  <strong>как системой роста GMV</strong>
</h1>
<p>
  Связываем привлечение новых клиентов, аналитику и каналы продаж с ростом GMV —
  а не только с показателями в рекламных отчётах
</p>
```

In the base Hero rules in `src/styles/global.css`, replace:

```css
.hero-copy > p { max-width: 780px; margin: 0 auto; color: rgba(255, 255, 255, 0.65); font-size: clamp(17px, 1.5vw, 22px); }
```

with:

```css
.hero-copy > p { max-width: 780px; margin: 0 auto; color: rgba(255, 255, 255, 0.65); font-size: 22px; }
```

Inside `@media (max-width: 640px)`, replace:

```css
.hero-copy > p { font-size: 16px; }
```

with:

```css
.hero-copy > p { font-size: 17px; }
```

Do not change the neighboring H1, CTA, metric, background, or spacing rules.

- [ ] **Step 4: Run the source tests and verify the Hero contract passes**

Run:

```bash
PATH=/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/override:$PATH npm test
```

Expected: PASS for every test in `v3-landing.test.mjs` and
`palitra-click-expand.test.mjs`.

- [ ] **Step 5: Run the full Astro and production-build validation**

Run:

```bash
PATH=/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/override:$PATH npm run check
PATH=/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/override:$PATH npm run build
PATH=/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/override:$PATH npm run test:build
```

Expected:

- `npm run check`: `0 errors`, `0 warnings`, `0 hints`.
- `npm run build`: all 12 static pages build successfully, including
  `/preview/hero/`.
- `npm run test:build`: every production HTML contract test passes.

- [ ] **Step 6: Inspect the isolated and full Hero visually**

Serve the verified `dist/` directory:

```bash
/Users/papenov/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m http.server 4321 --bind 127.0.0.1 --directory dist
```

If port `4321` is already serving this project, reuse it and reload after the
new build. In the in-app browser, inspect both URLs:

```text
http://127.0.0.1:4321/preview/hero/
http://127.0.0.1:4321/
```

Check `1440×900`, the annotated `1051×913`, and `390×844`. Confirm:

- tablet and desktop show the two intended headline phrases as the two
  hierarchy levels, with the second phrase violet;
- mobile preserves readable natural wrapping without horizontal overflow or
  clipped text;
- the supporting message is `22px` above `640px`, `17px` at `640px` and below,
  and has no final period;
- CTA focus remains visible and the CTA still targets `#lead`;
- all three proof metrics remain unchanged;
- the console has no errors and the page has no missing assets.

- [ ] **Step 7: Commit the tested implementation**

Run:

```bash
git add ../tests/v3-landing.test.mjs src/components/Hero.astro src/styles/global.css
git commit -m "feat(v3): refresh hero positioning copy"
```

Expected: one commit containing only the Hero structural test, shared component,
and Hero-specific CSS changes.

